# @berkayorhan/stackkit

CLI for generating and maintaining Stackkit monorepos.

```bash
npx @berkayorhan/stackkit create my-app --web next --api fastapi --db postgres --auth auth0
```

The package exposes the `stackkit` binary.
